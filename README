Afin de faire fonctionner le projet :

Dossier frontend :

- Dans le terminal : npm install && npm run start

Dossier Backend :

- Dans le terminal : npm install
- Dans le terminal : nodemon app
- Dans le dossier .env : écrire un exemple de token (Voir fichier .env.exemple). Egalement pour cryptoJS.
- Dans le dossier .env : inscrire son lien mongoDB afin de faire le lien avec votre base de donnée (sur le site MongoDB => Datebase => Connect => Connect your application)
